# Runtime SQLite state (agent/history/models DBs + WAL/SHM sidecars)
*.db
*.db-shm
*.db-wal

# Conversation & terminal session state
/agent/sessions/
/agent/terminal-sessions/
/agent/blobs/
/agent/cache/
/agent/history*
/agent/memories/
/agent/node_modules/

cache/

install-id

natives/
puppeteer/
run/
webcache/

# Logs
/logs/

# Hardware probe cache & runtime markers
/gpu_cache.json
/agent/last-changelog-version

# Extension dependencies
node_modules/
# Claude Code local state (not part of the harness config)
/.claude/

# Stale skill docs for tools removed from the extension stack.
# The tools/*.md entries were dropped 2026-08-19: those files are now deleted
# outright, and `glob.md` is tracked again — `glob` is a real current tool
# (omp renamed `find` to `glob` in 16.2.x).
/agent/skills/protocols/cite_before_answer.md
